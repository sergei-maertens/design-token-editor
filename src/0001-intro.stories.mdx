import {Meta} from '@storybook/addon-docs';

import ownTokens from '../lib/tokens/tokens';
import TokensTable from './TokensTable';

<Meta title="Introduction" />

# design-token-editor

The design token editor is meant to provide an interface to view and edit available
design token values from one or multiple component packages built with style-dictionary.

## Editor theme

The editor itself can be styled using design tokens. See the below table for available
tokens and their defaults.

<TokensTable container={ownTokens} autoExpand />
