import {Canvas, Meta, Story, ArgsTable} from '@storybook/addon-docs';
import ofDesignTokens from '@open-formulieren/design-tokens/dist/tokens.js';

import TokenRow from './TokenRow';

<Meta title="TokenRow" component={TokenRow} />

## Token row

export const Template = ({designToken}) => <TokenRow designToken={designToken} />;

<Canvas>
  <Story
    name="Default"
    args={{
      designToken: ofDesignTokens.of['page-footer'].bg,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={TokenRow} />
