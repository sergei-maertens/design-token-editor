import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';

import { TokenRow } from './index';

<Meta title="TokenRow" component={TokenRow} />

export const Template = (args) => <TokenRow prefix={args.prefix} />;

<Canvas>
  <Story name="Default" args={{
    prefix: 'Prefix'
  }}>
  {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={TokenRow} />
